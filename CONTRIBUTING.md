# Contributing

Open an issue before changing the canonical event mapping. Run `npm ci && npm run check`, do not commit credentials or customer data, and preserve backward compatibility with `org.govp.exchange/1`.
