# Security

Report vulnerabilities privately to `security@gemacode.org`. Never attach SAP credentials, cookies, database names, customer documents or GOVP connector tokens to an issue. Production deployments must use a secret store, HTTPS, a dedicated least-privilege SAP user and a transactional implementation of `B1GovpStore`.
